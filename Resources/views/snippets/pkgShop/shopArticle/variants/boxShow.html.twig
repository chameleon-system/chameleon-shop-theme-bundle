{#
    description: shows the available sizes an article is available in.
    vars
        sActiveSizeId
        aSizeList = array(
            array('id'=>'','name'=>'', 'selectLink'=>''),
            ..
        )
#}
{% extends "/pkgShop/shopArticle/variants/boxSelection.html.twig" %}
{% block variantValue%}
    <span{% if aValue.bArticleIsActive == '0' %} class="inactive"{% endif %}>{{aValue.sTitle}}</span>
{% endblock %}
{%block seclectionInfo %}{% endblock %}